# stubfetch (Python)

This directory is the **Python** distribution of [stubfetch](../README.md). The **canonical README** (install for npm + pip, quick starts, features, and docs index) lives at the **repository root**: [`../README.md`](../README.md).

```bash
pip install -e ./python
pip install -e '.[dev]' && pytest   # dev + tests
```

Python-specific reference: **[slaps.dev/docs/stubfetch/python](https://slaps.dev/docs/stubfetch/python)**.
