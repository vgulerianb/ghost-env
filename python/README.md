# ghost-env (Python)

This directory is the **Python** distribution of [ghost-env](../README.md). The **canonical README** (install for npm + pip, quick starts, features, and docs index) lives at the **repository root**: [`../README.md`](../README.md).

```bash
pip install -e ./python
pip install -e '.[dev]' && pytest   # dev + tests
```

Python-specific reference: **[slaps.dev/docs/ghost-env/python](https://slaps.dev/docs/ghost-env/python)**.
